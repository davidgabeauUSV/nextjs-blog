import { PayloadTransport } from '../core/payload-transport';
import { JsonRpcRequestPayload } from '../types';
import { IframeController } from '../core/iframe-controller';
export declare abstract class BaseModule {
    private readonly getTransport;
    private readonly getOverlay;
    constructor(getTransport: () => PayloadTransport, getOverlay: () => IframeController);
    protected get transport(): PayloadTransport;
    protected get overlay(): IframeController;
    protected request<ResultType = any>(payload: JsonRpcRequestPayload): Promise<ResultType>;
}
